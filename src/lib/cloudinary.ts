/**
 * @deprecated Uploads are stored in MongoDB via /api/upload.
 * Kept for reference only — do not use in production paths.
 */
export async function deleteImage(): Promise<void> {
  // no-op — legacy helper
}
